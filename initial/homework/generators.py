"""
Завдання: Генератор чисел з умовою

Мета:
    Освіжити знання про генератори в Python та їхню роль у роботі з памʼяттю.

Умова:
    Реалізуйте генератор, який повертає всі числа від 0 до n включно, що діляться одночасно на 5 і на 7.

    Вхідні дані:
        n - ціле число, що задає верхню межу діапазону.

    Вихідні дані:
        Генератор, який послідовно yield-ить числа, що задовольняють умову.

Вимоги:
    - Реалізація має бути через generator (yield);
    - Заборонено використовувати глобальні змінні;
    - Функція не повинна нічого друкувати.

Приклад:
    >>> list(generate_numbers(100))
    [0, 35, 70]

Аналітична частина:
    У докстрінгу функції додай відповіді на питання (коротко):
    1. Яка часова складність алгоритму за Big-O нотацією? І чому?
    2. Яка просторова складність за Big-O нотацією? І чому?
"""

from typing import Generator


def generate_numbers(n: int) -> Generator[int, None, None]:
    """
    1. O(n), бо треба перевірити кожне число
    2. O(1), бо віддає по одному
    """
    for i in range(n + 1):
        if i % 5 == 0 and i % 7 == 0:
            yield i
